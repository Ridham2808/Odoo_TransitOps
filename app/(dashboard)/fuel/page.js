export const metadata = { title: "Fuel & Expenses" };
export default function FuelPage() {
  return (
    <div>
      <h1 className="text-heading">Fuel & Expenses</h1>
      <p style={{ color: "var(--muted)", marginTop: 6, fontSize: 13 }}>
        Fuel logs & cost tracking — coming in Phase 1.
      </p>
    </div>
  );
}
