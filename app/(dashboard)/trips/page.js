export const metadata = { title: "Trips" };
export default function TripsPage() {
  return (
    <div>
      <h1 className="text-heading">Trip Dispatch</h1>
      <p style={{ color: "var(--muted)", marginTop: 6, fontSize: 13 }}>
        Trip lifecycle & dispatch — coming in Phase 1.
      </p>
    </div>
  );
}
